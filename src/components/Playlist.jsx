function Playlist() {
  const handleSave = () => {

  };

  return (
    <>
      <button onClick={handleSave} style={{display: 'inline', padding: 20, width: 200, margin: '20px auto'}}>Save To Spotify</button>
    </>
  );
}

export default Playlist;
